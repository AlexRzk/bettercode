---
title: CLI
description: BetterCode CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The BetterCode CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
bettercode
```

But it also accepts commands as documented on this page. This allows you to interact with BetterCode programmatically.

```bash
bettercode run "Explain how closures work in JavaScript"
```

---

### tui

Start the BetterCode terminal user interface.

```bash
bettercode [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The BetterCode CLI also has the following commands.

---

### agent

Manage agents for BetterCode.

```bash
bettercode agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
bettercode agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.bettercode/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
bettercode agent list
```

---

### attach

Attach a terminal to an already running BetterCode backend server started via `serve` or `web` commands.

```bash
bettercode attach [url]
```

This allows using the TUI with a remote BetterCode backend. For example:

```bash
# Start the backend server for web/mobile access
bettercode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
bettercode attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `BETTERCODE_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `BETTERCODE_SERVER_USERNAME` or `bettercode`) |

---

### auth

Command to manage credentials and login for providers.

```bash
bettercode auth [command]
```

---

#### login

BetterCode is powered by the provider list at [Models.dev](https://models.dev), so you can use `bettercode auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/bettercode/auth.json`.

```bash
bettercode auth login
```

When BetterCode starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
bettercode auth list
```

Or the short version.

```bash
bettercode auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
bettercode auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
bettercode github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
bettercode github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
bettercode github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
bettercode mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
bettercode mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
bettercode mcp list
```

Or use the short version.

```bash
bettercode mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
bettercode mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
bettercode mcp auth list
```

Or use the short version.

```bash
bettercode mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
bettercode mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
bettercode mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
bettercode models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
bettercode models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in BetterCode.

```bash
bettercode models --refresh
```

---

### run

Run bettercode in non-interactive mode by passing a prompt directly.

```bash
bettercode run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "bettercode run"
bettercode run Explain the use of context in Go
```

You can also attach to a running `bettercode serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
bettercode serve

# In another terminal, run commands that attach to it
bettercode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                                         | Short | Description                                                                |
| ------------------------------------------------------------ | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>                      |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr>                     | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>                      | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>                         |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>                        |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>                        | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>                        |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>                         | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>                       |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>                        |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>                       |       | Attach to a running bettercode server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr>                     | `-p`  | Basic auth password (defaults to `BETTERCODE_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr>                     | `-u`  | Basic auth username (defaults to `BETTERCODE_SERVER_USERNAME` or `bettercode`) |
| <nobr><code>{"--dir"}</code></nobr>                          |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>                         |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>                      |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr>                     |       | Show thinking blocks                                                       |
| <nobr><code>{"--dangerously-skip-permissions"}</code></nobr> |       | Auto-approve permissions that are not explicitly denied (dangerous!)       |

---

### serve

Start a headless BetterCode server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
bettercode serve
```

This starts an HTTP server that provides API access to bettercode functionality without the TUI interface. Set `BETTERCODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `bettercode`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage BetterCode sessions.

```bash
bettercode session [command]
```

---

#### list

List all BetterCode sessions.

```bash
bettercode session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an BetterCode session.

```bash
bettercode session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your BetterCode sessions.

```bash
bettercode stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
bettercode export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or BetterCode share URL.

```bash
bettercode import <file>
```

You can import from a local file or an BetterCode share URL.

```bash
bettercode import session.json
bettercode import https://opncd.ai/s/abc123
```

---

### web

Start a headless BetterCode server with a web interface.

```bash
bettercode web
```

This starts an HTTP server and opens a web browser to access BetterCode through a web interface. Set `BETTERCODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `bettercode`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
bettercode acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
bettercode plugin <module>
```

Or use the alias.

```bash
bettercode plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run BetterCode.

```bash
bettercode pr <number>
```

---

### db

Database tools.

```bash
bettercode db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
bettercode db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
bettercode debug [command]
```

---

### uninstall

Uninstall BetterCode and remove all related files.

```bash
bettercode uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates bettercode to the latest version or a specific version.

```bash
bettercode upgrade [target]
```

To upgrade to the latest version.

```bash
bettercode upgrade
```

To upgrade to a specific version.

```bash
bettercode upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The bettercode CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

BetterCode can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `BETTERCODE_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `BETTERCODE_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `BETTERCODE_CONFIG`                     | string  | Path to config file                               |
| `BETTERCODE_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `BETTERCODE_CONFIG_DIR`                 | string  | Path to config directory                          |
| `BETTERCODE_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `BETTERCODE_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `BETTERCODE_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `BETTERCODE_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `BETTERCODE_PERMISSION`                 | string  | Inlined json permissions config                   |
| `BETTERCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `BETTERCODE_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `BETTERCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `BETTERCODE_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `BETTERCODE_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `BETTERCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `BETTERCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `BETTERCODE_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `BETTERCODE_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `BETTERCODE_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `BETTERCODE_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `BETTERCODE_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `BETTERCODE_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `BETTERCODE_SERVER_USERNAME`            | string  | Override basic auth username (default `bettercode`) |
| `BETTERCODE_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `BETTERCODE_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `BETTERCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `BETTERCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `BETTERCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `BETTERCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `BETTERCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `BETTERCODE_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `BETTERCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `BETTERCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `BETTERCODE_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `BETTERCODE_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `BETTERCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `BETTERCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `BETTERCODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `BETTERCODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `BETTERCODE_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `BETTERCODE_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `BETTERCODE_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
