{
  "$schema": "https://v2-8-13.turborepo.dev/schema.json",
  "globalEnv": ["CI", "BETTERCODE_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "BETTERCODE_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "bettercode#test": {
      "dependsOn": ["^build"],
      "outputs": [],
      "passThroughEnv": ["*"]
    },
    "@bettercode/core#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@bettercode/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@bettercode/ui#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
